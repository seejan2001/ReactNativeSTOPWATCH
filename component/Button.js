import { View, TouchableOpacity } from "react-native";

export default Button = (props) => {
  return (
    <View>
      <TouchableOpacity>
        <Text>click</Text>
      </TouchableOpacity>
    </View>
  );
};
